<%= form_with(model: personality_trait) do |form| %>
  <% if personality_trait.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(personality_trait.errors.count, "error") %> prohibited this personality_trait from being saved:</h2>

      <ul>
        <% personality_trait.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
